<h1>Recipes:</h1>
{{#if recipes.length}}
  <div class="col-md-6 recipe-list">
    <h3>Current Recipes:</h3>

    {{#each recipes as |recipe|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4 href="/recipe/{{recipe.id}}">{{recipe.category}}</h4>
        <h4 href="/recipe/{{recipe.id}}">{{recipe.name}}</h4>
        <h4 href="/recipe/{{recipe.id}}">{{recipe.ingredients}}</h4>
        <h4 href="/recipe/{{recipe.id}}">{{recipe.description}}</h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{recipe.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

  <p>Created by {{user.name}} on</p>

  <script src="./js/recipeLogic.js"></script>